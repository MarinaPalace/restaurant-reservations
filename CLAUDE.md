@AGENTS.md

# Vista Del Mar reservations

Read **HANDOVER.md** before changing anything. It records the decisions behind this codebase,
the rules that must not be broken (local-only date handling, additive-only schema changes,
menu isolation, authorisation in the route rather than the UI, seat accounting), and what is
still open before beta.

`README.md` covers running and using the app.
